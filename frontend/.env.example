# Base URL of the SimpleInvoice backend API.
# Local (without Docker): http://localhost:3000
# Docker Compose maps the backend to the host on port 3000 as well.
VITE_API_BASE_URL=http://localhost:3000
